# frontend - SiKecilKuat

## How To Install 
- first you need to install nodejs on your machine
- second you need to download backend from this repo and run them with the instruction inside the backend repo
- then you can download this repo and inside this folder you can run command ``` npm install ``` on the terminal
- finally you can run app with run this command on the terminal ``` npm run start ``` 
